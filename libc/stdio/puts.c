#include <stdio.h>
int puts(const char* string) {
	return printf("%s\n", string);
}

// puts.c - pfffff lol this is the smallest one I've ever done. It's a simple printf wrapper that prints a clean string.